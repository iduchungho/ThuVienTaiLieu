import React from 'react';
import CheckoutForm from './CheckoutForm';

const Checkout = () => {
  return (
    <div className=" h-screen bg-gradient-to-r from-purple-600 to-pink-600">
      <CheckoutForm />
    </div>
  );
};

export default Checkout;
